pipeline {
agent any

    stages {
        stage('Checkout') {
        steps{
            
        }
        }

        stage('Install Dependencies') {
            steps{

                
            }
        }

        stage('Run Tests') {
          steps{}
        }
}
}
