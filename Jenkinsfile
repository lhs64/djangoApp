pipeline {
agent any

    stages {
        stage('Checkout') {
        
        }

        stage('Install Dependencies') {
            
        }

        stage('Run Tests') {
          
        }
}
}
